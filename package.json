{
  "name": "vcrew",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "node --experimental-sqlite backend/src/server.js",
    "seed": "node --experimental-sqlite backend/src/seed.js"
  }
}
