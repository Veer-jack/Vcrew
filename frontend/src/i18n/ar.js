// AR — TODO: needs native speaker translation
// Structure matches en.js exactly. Replace all English values with ar translations.
// IMPORTANT: Arabic (ar) and Urdu (ur) are RTL languages — the app automatically
// applies dir="rtl" when these are active; no extra CSS needed in this file.

import en from "./en.js";

// Temporary stub: falls back to English for all strings.
// Replace each value with the correct ar translation.
const ar = { ...en };

export default ar;
